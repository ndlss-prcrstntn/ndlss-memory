# Roadmap 0.2.1

Release snapshot for `0.2.1`.

Focus of this patch release: startup readiness reliability and operational clarity.

## Completed in 0.2.1

- [x] Startup preflight checks before runtime enters ready state.
- [x] Qdrant reachability validation at startup.
- [x] Workspace existence/readability validation at startup.
- [x] Git availability validation for `INDEX_MODE=delta-after-commit`.
- [x] Fail-fast startup behavior with structured machine-readable errors.
- [x] Unified startup readiness summary:
  - [x] `GET /v1/system/startup/readiness`
  - [x] startup diagnostics in `GET /v1/system/config`
  - [x] ready summary logs in `mcp-server` and `file-indexer`
- [x] Compose and env wiring for startup preflight flags across presets.
- [x] Startup-preflight unit/integration/contract/smoke coverage.
- [x] Quality pipeline stabilization for integration regression sequence.

## Release outcomes

- [x] `VERSION` bumped to `0.2.1`
- [x] `CHANGELOG.md` updated
- [x] roadmap snapshot added and linked from docs
- [x] full quality run passed
