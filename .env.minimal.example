# Minimal local setup (root docker-compose.yml)
# Most parameters have sane defaults in compose and service entrypoints.

# Optional: override workspace mount for root compose file.
ROOT_HOST_WORKSPACE_PATH=.

# Optional: expose mcp-server on another port.
# MCP_PORT=8080

# Optional: switch index mode.
# INDEX_MODE=full-scan
